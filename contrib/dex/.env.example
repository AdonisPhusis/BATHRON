# LP Watcher Configuration
# Copy this to .env and fill in your values
# NEVER commit .env to git!
# chmod 600 .env (owner read/write only)

# LP's EVM private key for auto-claiming USDC
# This is the private key of the address that receives USDC locks
# (Must match LP_POLYGON_ADDRESS in lp_watcher.py)
# Use a DEDICATED hot wallet address, NOT your main wallet!
LP_EVM_PRIVKEY=your_private_key_here_without_0x_prefix

# Auto-claim toggle (safety switch)
# Set to 0 to log preimages without auto-claiming (safe mode)
# Set to 1 to automatically claim USDC when preimage is revealed
AUTO_CLAIM=1
