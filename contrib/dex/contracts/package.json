{
  "name": "piv2-htlc-contracts",
  "version": "1.0.0",
  "description": "PIV2 HTLC Smart Contracts for cross-chain atomic swaps",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "test:coverage": "hardhat coverage",
    "deploy:localhost": "hardhat run scripts/deploy.js --network localhost",
    "deploy:polygon": "hardhat run scripts/deploy.js --network polygon",
    "deploy:polygon-amoy": "hardhat run scripts/deploy.js --network polygon_amoy",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "verify": "hardhat verify",
    "clean": "hardhat clean",
    "node": "hardhat node"
  },
  "keywords": [
    "htlc",
    "atomic-swap",
    "piv2",
    "pivx",
    "cross-chain",
    "defi"
  ],
  "author": "PIV2 Team",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.0"
  }
}
